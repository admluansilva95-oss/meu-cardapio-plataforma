# Copie para `.env.e2e` ou defina em `.env.local` (carregados por `tests/e2e/global-setup.ts`).
# Obrigatório para os 6 testes de integração (login + /admin + logout).
E2E_EMAIL=
E2E_PASSWORD=

# Opcional: tenant explícito em `/admin?slug=...`
# E2E_RESTAURANT_SLUG=
